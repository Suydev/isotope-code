FILE IDENTITY
  Path: /tmp/isotope-repo/performance-patch.sql
  Audit Path: audit/performance-patch.sql.txt
  Commit: 095c2fcf03b5a1a30eddecc0a372c39723a6cfa1
  Tracked: Yes
  Type: SQL Script
  MIME: text/x-sql
  Size: 14914 bytes
  SHA-256: d16577cd84c806e7a09b5d44bdfa92a1e3e4b53e5bcdadd73510cf0152b4d849

PURPOSE
  Optimizes performance via covering indexes, missing FK indexes, a security-definer helper for group membership, and optimized RLS policies.

SQL OBJECTS DEFINED
  - Indexes: idx_gm_user_group_covering (composite), idx_ginv_created_by, idx_gann_author, idx_gchall_created_by, idx_gcpart_challenge_user, idx_inventory_user, idx_presence_updated_at, idx_sessions_user_started, idx_daily_user_date_minutes.
  - Function: get_my_group_ids() (SECURITY DEFINER, STABLE).
  - Policies: Re-creates many policies using `(SELECT auth.uid())` instead of `auth.uid()` for performance.

MIGRATION ORDER AND IDEMPOTENCY
  - Order: Run after isotope-complete.sql or community-patch-v4.sql.
  - Idempotency: Fully idempotent; uses `IF NOT EXISTS` and `DROP POLICY IF EXISTS`.

SECURITY ANALYSIS
  - RLS Policies: Updated to use a more efficient evaluation pattern.
  - SECURITY DEFINER: get_my_group_ids() uses it correctly with `SET search_path = public`.
  - Optimization: Uses `(SELECT auth.uid())` to force single evaluation per query, a known Supabase performance optimization.

FINDINGS
  - SQL-010; Severity: INFO; Evidence: Implements the "Supabase Advisor" fix for RLS overhead.
  - SQL-011; Severity: INFO; Evidence: Uses a covering index for the group membership subquery to allow index-only scans.

FINAL VERDICT
  - PASS: Excellent set of performance and security optimizations.
