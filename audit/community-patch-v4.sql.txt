FILE IDENTITY
  Path: /tmp/isotope-repo/community-patch-v4.sql
  Audit Path: audit/community-patch-v4.sql.txt
  Commit: 095c2fcf03b5a1a30eddecc0a372c39723a6cfa1
  Tracked: Yes
  Type: SQL Script
  MIME: text/x-sql
  Size: 121464 bytes
  SHA-256: f264a0a702da4a6d8e9199c0e7edecc83e613e50b78e72a977310e5f35ccf459

PURPOSE
  A comprehensive patch (v4) to fix mismatches between the frontend JS and the database schema. It updates tables, creates missing ones, and replaces RPC functions.

SQL OBJECTS DEFINED
  - Updates many tables: users, groups, group_members, group_chat_messages, group_invites, group_announcements, group_milestones, user_stats_summary, user_points, user_profiles.
  - Creates tables: group_challenges, group_challenge_participants, daily_user_stats, study_sessions_log, store_items, user_inventory, notifications, user_presence, user_tours.
  - RPCs: Replaces/Updates many functions (get_invite_details, accept_invite, etc.).
  - Storage: Creates buckets (user-content, avatars, notes) and sets RLS policies.
  - Functions: set_user_tours_updated_at().
  - Triggers: trg_user_tours_updated_at.

MIGRATION ORDER AND IDEMPOTENCY
  - Order: Run on existing installs to sync DB with v4 frontend.
  - Idempotency: Fully idempotent; uses `IF NOT EXISTS`, `DROP FUNCTION IF EXISTS`, and `DO $$` blocks.

SECURITY ANALYSIS
  - RLS Policies: Recreates/Adds policies for many tables and storage objects.
  - SECURITY DEFINER: Used for functions with `SET search_path = public` (where visible).
  - Storage Security: Implements per-user path-based access control for storage buckets.

FINDINGS
  - SQL-005; Severity: INFO; Evidence: Fixes a critical mismatch where JS used `user_id` instead of `sender_id` in `group_chat_messages`.
  - SQL-006; Severity: INFO; Evidence: Correctly handles Storage bucket RLS using `split_part(name, '/', 1)`.

FINAL VERDICT
  - PASS: Essential for maintaining compatibility between the application code and the database.
