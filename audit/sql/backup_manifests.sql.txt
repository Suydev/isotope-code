FILE IDENTITY
  Path: /tmp/isotope-repo/sql/backup_manifests.sql
  Audit Path: audit/sql/backup_manifests.sql.txt
  Commit: 095c2fcf03b5a1a30eddecc0a372c39723a6cfa1
  Tracked: Yes
  Type: SQL Script
  MIME: text/x-sql
  Size: 2165 bytes
  SHA-256: d65eccd899f5880d9e74d4d6adae2cb89cff444a0431e394debfe13916c0bf57

PURPOSE
  Sets up the table and security for tracking user backup manifests.

SQL OBJECTS DEFINED
  - Table: public.backup_manifests (id, user_id, bucket, path, kind, content_hash, size_bytes, collection_counts, exported_at, selected_as_best, score, created_at, updated_at).
  - Constraints: backup_manifests_path_user_prefix (ensures path starts with user_id).
  - Indexes: backup_manifests_bucket_path_idx (unique), backup_manifests_user_score_idx.
  - RLS Policies: backup_manifests_select_own, backup_manifests_insert_own, backup_manifests_update_own, backup_manifests_delete_own.
  - Grants: Grants to authenticated and service_role.

MIGRATION ORDER AND IDEMPOTENCY
  - Order: Backup system setup.
  - Idempotency: Uses `create table if not exists` and `drop policy if exists`.

SECURITY ANALYSIS
  - RLS: Strictly enforces that users can only interact with their own backup manifests.
  - Constraint: `check (split_part(path, '/', 1) = user_id::text)` adds a second layer of security for the storage path.

FINDINGS
  - SQL-014; Severity: INFO; Evidence: Well-secured table with both RLS and CHECK constraints.

FINAL VERDICT
  - PASS: Secure and well-structured implementation of the backup manifest system.
