FILE IDENTITY

- original path: sql/backup_manifests.sql
- audit path: audit/sql/backup_manifests.sql.txt
- audited commit: 34c3836e45030eb67c4e0f0bf654d30db0509b89
- tracked or untracked: tracked (tracked)
- file type: sql
- MIME type: application/sql
- size in bytes: 2165
- SHA-256: d65eccd899f5880d9e74d4d6adae2cb89cff444a0431e394debfe13916c0bf57
- text encoding when applicable: utf-8 or ascii-compatible text
- executable status: not executable
- generated/minified status: authored or hand-maintained
- primary subsystem: offline/sync/backup
- primary auditor: sql-supabase-agent
- review status: lead-generated; cross-cutting static review applied
- confidence: MEDIUM

PURPOSE

Database schema, migration, policy, or validation artifact for Supabase/Postgres behavior.

ROLE IN THE APPLICATION

- subsystem: offline/sync/backup
- runtime stage: database provisioning/reference
- user-visible feature: indirect or internal
- whether it is active: referenced
- whether it is generated: authored or hand-maintained
- whether it is comparison/reference material: not primarily
- whether it is legacy: no direct legacy-only signal
- whether it appears unused: not proven unused

REFERENCES AND CONSUMERS

Confirmed imports, importers, HTML references, CSS references, manifest references, service-worker references, script references, installer references, documentation references, filesystem reads, route consumers, and database consumers found by direct search:
- .github/workflows/ci.yml:204 - sql/backup_manifests.sql
- .github/workflows/ci.yml:253 - for f in isotope-complete.sql community-patch-v4.sql sql/006_security_policy_cleanup.sql sql/backup_manifests.sql sql/verify-security.sql performance-patch.sql; do
- .github/workflows/ci.yml:335 - for f in server.mjs package.json setup.sh install-termux.sh bin/isotope .env.example public/sync/backup-normalizer.js public/sync/local-data-adapter.js server/backup-manager.mjs scripts/prove-new-browser-restore.mjs sql/backup_manifests.sql
- .github/workflows/regenerate-release.yml:108 - sql/backup_manifests.sql
- .github/workflows/release.yml:183 - sql/backup_manifests.sql
- docs/supabase-connection-map.md:38 - - `sql/backup_manifests.sql`

INTERNAL STRUCTURE

- line count: 52
- imports:
None observed.
- exports:
None observed.
- functions/async flows:
None observed.
- classes:
None observed.
- constants/mutable state candidates:
None observed.
- handlers/lifecycle candidates:
None observed.

INPUTS

- parameters: not directly observed
- environment variables:
No environment variable names observed.
- command-line arguments: not directly observed
- request data: not directly observed
- headers/cookies: not directly observed
- files: not directly observed
- database data:
- "backup_manifests_delete_own" on public.backup_manifests
- "backup_manifests_insert_own" on public.backup_manifests
- "backup_manifests_select_own" on public.backup_manifests
- "backup_manifests_update_own" on public.backup_manifests
- public.backup_manifests
- network responses:
None observed.
- browser storage:
None observed.
- user interactions: not directly observed

OUTPUTS AND SIDE EFFECTS

- 17: updated_at timestamptz not null default now(),
- 26: on public.backup_manifests(user_id, selected_as_best desc, score desc, updated_at desc);
- 35: drop policy if exists "backup_manifests_insert_own" on public.backup_manifests;
- 36: create policy "backup_manifests_insert_own"
- 37: on public.backup_manifests for insert
- 40: drop policy if exists "backup_manifests_update_own" on public.backup_manifests;
- 41: create policy "backup_manifests_update_own"
- 42: on public.backup_manifests for update
- 46: drop policy if exists "backup_manifests_delete_own" on public.backup_manifests;
- 47: create policy "backup_manifests_delete_own"
- 48: on public.backup_manifests for delete
- 51: grant select, insert, update, delete on public.backup_manifests to authenticated;

DATA FLOW

SQL definitions flow from repository migration/reference files into Supabase/Postgres only when explicitly applied outside this audit.

DEPENDENCIES

- platform dependencies: none beyond repository/runtime context observed
- Node core modules:
- 8: path text not null,
- 19: check (split_part(path, '/', 1) = user_id::text)
- 23: on public.backup_manifests(bucket, path);
- 38: with check ((select auth.uid()) = user_id and split_part(path, '/', 1) = (select auth.uid())::text);
- 44: with check ((select auth.uid()) = user_id and split_part(path, '/', 1) = (select auth.uid())::text);
- external libraries:
None observed.
- local files: see References and Consumers above
- browser APIs:
None observed.
- operating-system commands:
None observed.
- Supabase interfaces:
- 2: -- Idempotent migration. Run in Supabase SQL Editor.
- external services: none observed

SECURITY AND PRIVACY

- Contains or references auth/session/secret-sensitive concepts; verify server-only boundaries and browser exposure.

CORRECTNESS AND FAILURE MODES

- Depends on asynchronous I/O or browser/server storage; failure modes include network loss, stale state, partial writes, and malformed data.

OFFLINE AND LOCAL-FIRST BEHAVIOUR

- Participates in offline/local-first behavior or references browser persistence/cache APIs.

PERFORMANCE

- No major static performance concern observed.

MAINTAINABILITY

- Maintained through existing repository structure; no direct maintainability issue confirmed.

FINDINGS

- finding ID: ISO-AUDIT-0570-01
  severity: INFO
  category: audit
  status: CONFIRMED
  confidence: MEDIUM
  affected lines or byte-level evidence: sql/backup_manifests.sql inspected; no file-specific high-risk issue confirmed
  observed evidence: sql/backup_manifests.sql inspected; no file-specific high-risk issue confirmed
  inferred impact: No direct issue was confirmed from static evidence in this file.
  reproduction or reasoning: Inspect the referenced path and line or byte metadata; no runtime mutation was used.
  recommended correction: Keep covered by relevant smoke/static tests.
  related files: .github/workflows/ci.yml, .github/workflows/ci.yml, .github/workflows/ci.yml
  regression risk: Review behavior against existing tests before changing.

POSITIVE OBSERVATIONS

- File was included through a deterministic manifest rather than filename-only guessing.
- Tracked by Git, which improves reproducibility of audit evidence.
- At least one direct reference/consumer was found.

RECOMMENDED TESTS

- Run SQL lint/idempotency checks against a disposable local database before applying anywhere.
- Keep this file covered by repository smoke tests or reference checks appropriate to its subsystem.

FINAL VERDICT

- role: Database schema, migration, policy, or validation artifact for Supabase/Postgres behavior.
- activity status: referenced
- reliability: MEDIUM; static audit only, runtime behavior should be verified by targeted tests.
- largest risk: subsystem-specific drift from implementation
- highest-value improvement: keep references/tests aligned with actual consumers
