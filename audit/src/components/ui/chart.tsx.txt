FILE IDENTITY

- original path: src/components/ui/chart.tsx
- audit path: audit/src/components/ui/chart.tsx.txt
- audited commit: 34c3836e45030eb67c4e0f0bf654d30db0509b89
- tracked or untracked: tracked (tracked)
- file type: code
- MIME type: text/tsx
- size in bytes: 10763
- SHA-256: b40d1236f168c746b228adec6bc4e9d196ea362a7910878f161d3c359a013b89
- text encoding when applicable: utf-8 or ascii-compatible text
- executable status: not executable
- generated/minified status: authored or hand-maintained
- primary subsystem: editable frontend source
- primary auditor: source-frontend-agent
- review status: lead-generated; cross-cutting static review applied
- confidence: MEDIUM

PURPOSE

Project file in the editable frontend source subsystem. Its role is determined from path, content, and references.

ROLE IN THE APPLICATION

- subsystem: editable frontend source
- runtime stage: development, installation, or documentation
- user-visible feature: indirect or internal
- whether it is active: source status depends on Vite build path; inspect references and bundle drift
- whether it is generated: authored or hand-maintained
- whether it is comparison/reference material: not primarily
- whether it is legacy: no direct legacy-only signal
- whether it appears unused: not proven unused

REFERENCES AND CONSUMERS

Confirmed imports, importers, HTML references, CSS references, manifest references, service-worker references, script references, installer references, documentation references, filesystem reads, route consumers, and database consumers found by direct search:
No direct textual consumers found by path/name search.

INTERNAL STRUCTURE

- line count: 367
- imports:
- 1: import * as React from "react"
- 2: import * as RechartsPrimitive from "recharts"
- 4: import { cn } from "@/lib/utils"
- exports:
- 9: export type ChartConfig = {
- 360: export {
- functions/async flows:
- 25: function useChart() {
- 28: if (!context) {
- 43: >(({ id, className, children, config, ...props }, ref) => {
- 68: const ChartStyle = ({ id, config }: { id: string; config: ChartConfig }) => {
- 73: if (!colorConfig.length) {
- 85: .map(([key, itemConfig]) => {
- 131: ) => {
- 134: const tooltipLabel = React.useMemo(() => {
- 135: if (hideLabel || !payload?.length) {
- 147: if (labelFormatter) {
- 155: if (!value) {
- 170: if (!active || !payload?.length) {
- 188: .map((item, index) => {
- 272: ) => {
- 275: if (!payload?.length) {
- 290: .map((item) => {
- 322: function getPayloadConfigFromPayload(
- 327: if (typeof payload !== "object" || payload === null) {
- classes:
None observed.
- constants/mutable state candidates:
- 7: const THEMES = { light: "", dark: ".dark" } as const
- 23: const ChartContext = React.createContext<ChartContextProps | null>(null)
- 26: const context = React.useContext(ChartContext)
- 35: const ChartContainer = React.forwardRef<
- 44: const uniqueId = React.useId()
- 45: const chartId = `chart-${id || uniqueId.replace(/:/g, "")}`
- 68: const ChartStyle = ({ id, config }: { id: string; config: ChartConfig }) => {
- 69: const colorConfig = Object.entries(config).filter(
- 86: const color =
- 101: const ChartTooltip = RechartsPrimitive.Tooltip
- 103: const ChartTooltipContent = React.forwardRef<
- 134: const tooltipLabel = React.useMemo(() => {
- 140: const key = `${labelKey || item?.dataKey || item?.name || "value"}`
- 141: const itemConfig = getPayloadConfigFromPayload(config, item, key)
- 142: const value =
- 174: const nestLabel = payload.length === 1 && indicator !== "dot"
- 189: const key = `${nameKey || item.name || item.dataKey || "value"}`
- 190: const itemConfig = getPayloadConfigFromPayload(config, item, key)
- 191: const indicatorColor = color || item.payload.fill || item.color
- 259: const ChartLegend = RechartsPrimitive.Legend
- 261: const ChartLegendContent = React.forwardRef<
- 291: const key = `${nameKey || item.dataKey || "value"}`
- 292: const itemConfig = getPayloadConfigFromPayload(config, item, key)
- 331: const payloadPayload =
- 338: let configLabelKey: string = key
- handlers/lifecycle candidates:
None observed.

INPUTS

- parameters: not directly observed
- environment variables:
- CSS_SELECTOR: classification=configuration; review sensitivity; required=unknown from static map; inspect use locations; uses=src/components/ui/chart.tsx:6
- THEMES: classification=configuration; review sensitivity; required=unknown from static map; inspect use locations; uses=src/components/ui/chart.tsx:7, src/components/ui/chart.tsx:15, src/components/ui/chart.tsx:80
- THEME_NAME: classification=configuration; review sensitivity; required=unknown from static map; inspect use locations; uses=src/components/ui/chart.tsx:6
- command-line arguments: not directly observed
- request data: not directly observed
- headers/cookies: not directly observed
- files: not directly observed
- database data:
None observed.
- network responses:
None observed.
- browser storage:
None observed.
- user interactions: browser/UI file may react to user actions

OUTPUTS AND SIDE EFFECTS

No direct outputs or side effects observed from static text; binary assets are served/read by consumers.

DATA FLOW

Data flow is indirect through imports, script execution, documentation, installation, or asset consumption as listed in references.

DEPENDENCIES

- platform dependencies: none beyond repository/runtime context observed
- Node core modules:
None observed.
- external libraries:
- 1: import * as React from "react"
- 2: import * as RechartsPrimitive from "recharts"
- 4: import { cn } from "@/lib/utils"
- local files: see References and Consumers above
- browser APIs:
None observed.
- operating-system commands:
None observed.
- Supabase interfaces:
None observed.
- external services: none observed

SECURITY AND PRIVACY

- No direct secret or browser-executable security issue confirmed from static inspection.

CORRECTNESS AND FAILURE MODES

- No complex runtime failure mode observed.

OFFLINE AND LOCAL-FIRST BEHAVIOUR

- No direct offline/local-first behavior observed.

PERFORMANCE

- No major static performance concern observed.

MAINTAINABILITY

- Maintained through existing repository structure; no direct maintainability issue confirmed.

FINDINGS

- finding ID: ISO-AUDIT-0586-01
  severity: INFO
  category: audit
  status: CONFIRMED
  confidence: MEDIUM
  affected lines or byte-level evidence: src/components/ui/chart.tsx inspected; no file-specific high-risk issue confirmed
  observed evidence: src/components/ui/chart.tsx inspected; no file-specific high-risk issue confirmed
  inferred impact: No direct issue was confirmed from static evidence in this file.
  reproduction or reasoning: Inspect the referenced path and line or byte metadata; no runtime mutation was used.
  recommended correction: Keep covered by relevant smoke/static tests.
  related files: See references and consumers above.
  regression risk: Review behavior against existing tests before changing.

POSITIVE OBSERVATIONS

- File was included through a deterministic manifest rather than filename-only guessing.
- Tracked by Git, which improves reproducibility of audit evidence.
- No direct consumer found; this uncertainty is explicitly recorded rather than hidden.

RECOMMENDED TESTS

- Keep this file covered by repository smoke tests or reference checks appropriate to its subsystem.

FINAL VERDICT

- role: Project file in the editable frontend source subsystem. Its role is determined from path, content, and references.
- activity status: source status depends on Vite build path; inspect references and bundle drift
- reliability: MEDIUM; static audit only, runtime behavior should be verified by targeted tests.
- largest risk: subsystem-specific drift from implementation
- highest-value improvement: keep references/tests aligned with actual consumers
